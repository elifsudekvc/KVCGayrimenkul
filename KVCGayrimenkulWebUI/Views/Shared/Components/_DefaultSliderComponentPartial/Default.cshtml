
<div class="slider-container" data-aos="fade-up">
    <div class="slider-content">
        <img src="~/Picture/slider/slider1.jpg"
             alt="Slider Image"
             class="sliderImage"
             id="sliderImage" />
        <div class="Title">
            Hayallerinizi Ertelemeyin..
            <div class="secondTitle color4">Güvenilir Gayrimenkul Çözümleri</div>
            <a href="/AdsPage" class="AdsLink color5 adsButton backgroundcolor4 btn btn-lg">Tüm İlanlar</a>
        </div>
        <button class="leftRight" id="prevBtn" data-aos="fade-left">
            <i class="fa-solid fa-chevron-left fa-2xl"></i>
        </button>
        <button class="leftRight" id="nextBtn" data-aos="fade-right">
            <i class="fa-solid fa-chevron-right fa-2xl"></i>
        </button>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
<script>
    AOS.init({
        duration: 800,  // Animasyon süresi (milisaniye)
        easing: 'ease-in-out',  // Animasyon yumuşatma ayarı
    });
</script>

<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
    }

    .slider-container {
        width: 100vw;
        height: 90vh;
    }

    .slider-content {
        position: relative;
        width: 100%;
        height: 100%;
        overflow: hidden;
    }


    .sliderImage {
        width: 100%;
        height: 100vh !important;
        object-fit: cover; /* Resmi kırpmadan orantılı şekilde yerleştirir */
        object-position: center; /* Resmin ortasını gösterir */
        transition: 1s ease;
    }

    
    @@media screen and (max-width: 1300px) {
        .Title {
            left: 50% !important;
            top: 72% !important;
        }
    }

    @@media screen and (max-width: 400px) {
        .kvcTitle {
            margin-left: 15px !important;
            font-size: large !important;
        }

        .nav-link {
            font-size: large !important;
        }

        .logo {
            display: none !important;
        }

        .Title {
            font-size: 1.6rem !important;
        }

        .secondTitle {
            font-size: 1rem !important;
        }

        .sliderImage {
            height: auto;
        }

        .aboutcontainer {
            padding: 1.5rem !important;
        }
    }

    .Title {
        position: absolute;
        top: 75%;
        left: 22%;
        transform: translate(-50%, -50%);
        color: #ffffff;
        font-size: 2.5rem;
        font-weight: bold;
        text-shadow: 2px 2px 4px rgb(0, 0, 0);
        background-color: #0d192688;
        padding: 20px;
        border-radius: 10px;
    }

    .secondTitle {
        font-weight: 100;
        font-size: 1.5rem;
    }

    .adsButton:hover {
        background-color: #0D1926 !important;
        color: #31d375 !important;
    }

    .AdsLink {
        font-size: x-large;
        width: 10rem;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 3rem;
        margin-top: 1rem;
    }

    .leftRight {
        background: none;
        border: none;
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        z-index: 1;
        cursor: pointer;
        height: 8vh;
        border-radius: 10px;
        background-color: #0d19268a !important;
    }


        .leftRight:hover {
            height: 12vh !important;
            transition: 1s ease !important
        }
    #prevBtn {
        left: 10px;
    }

    #nextBtn {
        right: 10px;
    }

    .col-12 {
        padding: 2.5rem;
    }

</style>


<script>
    const images = [
        "/Picture/slider/slider1.jpg",
        "/Picture/slider/slider2.jpg",
        "/Picture/slider/slider3.jpg"
    ];
    let currentImageIndex = 0;

    const sliderImage = document.getElementById('sliderImage');
    const prevBtn = document.getElementById('prevBtn');
    const nextBtn = document.getElementById('nextBtn');

    const changeImage = () => {
        sliderImage.src = images[currentImageIndex];
    };

    const handlePrev = () => {
        currentImageIndex = (currentImageIndex - 1 + images.length) % images.length;
        changeImage();
    };

    const handleNext = () => {
        currentImageIndex = (currentImageIndex + 1) % images.length;
        changeImage();
    };

    setInterval(handleNext, 5000);

    prevBtn.addEventListener('click', handlePrev);
    nextBtn.addEventListener('click', handleNext);

</script>